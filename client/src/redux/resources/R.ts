export default class R {
  static server = {
    cloud: "http://localhost:3000/api/",
  };

  static fonts = {};

  static url = {
    user: {
      login: "auth/login",
      registration: "auth/registration",
      auth: "auth/auth",
      files: '/files',
    },
  };

  static string = {};

  static colors = {};
}
